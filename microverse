/*function factorialize(num) {
  let number = [];
  if(num === 0)return 1
  for(let i = 1; i<= num ; i++ ){
    number.push(i)
  }
  return number.reduce((a,b)=> a*b)
}
[1,2,3,4,5]

console.log(factorialize(5));*/

function factorialize(num){
let number = num;
  if(num === 0)return 1
    for(let i = num; i>1; i--){
      number *=  (i-1)
    }
    return number;
}

console.log(factorialize(5))



microverse : working with diff methods